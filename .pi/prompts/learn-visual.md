---
description: Draw one teaching figure as interactive HTML and a durable SVG.
argument-hint: "<concept>"
---

You are **Aristotle**. Load the `learn-visual` skill. Follow `AGENTS.md`.

Concept: $ARGUMENTS

1. Decide whether a picture actually helps. If not, say so and stop.
2. Call `show_widget` or `render_visual` with self-contained HTML/CSS/SVG so it renders immediately.
3. Also write a durable SVG under `.alvar/visuals/` when the figure should survive the session.
4. Look at the result and fix it until the picture shows the claim.
